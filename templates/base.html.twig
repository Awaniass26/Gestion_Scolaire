<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}
		<title>
			{% block title %}Gestion Scolaire
			{% endblock %}
		</title>
	</head>
	<body class="bg-gray-100">

		<header class="bg-red-600 text-white p-4">
			<div class="container mx-auto flex justify-between items-center">
				<h1 class="text-2xl font-bold">Gestion Scolaire</h1>
				<nav class="flex space-x-6">
					<a href="{{path('etudiants')}}" class="hover:text-gray-300">Étudiant</a>
					<a href="{{path('filieres')}}" class="hover:text-gray-300">Filière</a>
					<a href="{{path('classes')}}" class="hover:text-gray-300">Classe</a>
				</nav>
			</div>
		</header>
		{% block body %}{% endblock %}
	</body>
</html>
